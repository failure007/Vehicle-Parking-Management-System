version: 2.1

jobs:
  build:
    docker:
      - image: circleci/php:8.2.4

    steps:
      - checkout
      - run:
          name: Install PHP Dependencies
          command: composer install

      - run:
          name: Run PHPUnit Tests
          command: ./vendor/bin/phpunit

workflows:
  version: 2
  my-workflow:
    jobs:
      - build